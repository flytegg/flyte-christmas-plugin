package gg.flyte.event.visual.hud

/**
 * Enum class used to indicate whether the element it's used for has to be aligned to the left, right, or center
 */
enum class Alignment {
    LEFT, RIGHT, CENTER
}